name: libusb_android_helper_example
description: "Demonstrates how to use the libusb_android_helper plugin."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.5.0
dependencies:
  flutter:
    sdk: flutter

  libusb_android_helper: 
    path: ../
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
